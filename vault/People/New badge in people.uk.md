Та сама дата, що й у двох прикладах у «Дописах», і так само немає значка.

Розділ «Люди» — це сітка обкладинок (`components/lists/PeopleCards.tsx`), де картка складається з фото та імені. Якщо значок і з'явиться тут, то як позначка на самій картці, а не як чип після заголовка — це та сама проблема, що й на полиці, і саме тому обидва розділи не потрапили до першої версії.

Скажи — і розширимо: `components/NewBadge.tsx` приймає лише `date`, тож його можна вмонтувати в будь-який список, де вона є.
